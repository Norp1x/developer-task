version: '3.8'

services:
  search-service:
    build:
      context: ./search-service
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    container_name: search-service-container

  google-service:
    build:
      context: ./google-service
      dockerfile: Dockerfile
    ports:
      - "8081:8081"
    container_name: google-service-container
